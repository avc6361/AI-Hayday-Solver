# CMPSC441-AI-HayDay-Solver
Resource and order fullfilment maximizer for Supercell's Hay Day.
Live demo: https://avc6361.github.io/AI-Hayday-Solver/

Note: Only a greedy solver is currently available.

## Documents
Assignment-related Typst documents are in `docs/`. Opening this project in VSCode will recommend a Typst extension, which can be used to preview these documents.

To open a live preview of a document, open the command pallete (`CTRL + SHIFT + P` or `⌘ + SHIFT + P`) and select:\
 `>Typst Preview: Preview Opened File`

## Development
1. Ensure NodeJS is installed on your system
```sh
node --version
```

2. Install dependencies
```sh
npm install
```

3. Start the live preview with Vite
```sh
npm run dev
```
